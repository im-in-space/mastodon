diff --cc scripts/install-to-mastodon.sh
index b62d8e1,b9c9778..0000000
--- a/scripts/install-to-mastodon.sh
+++ b/scripts/install-to-mastodon.sh
@@@ -23,8 -19,7 +23,8 @@@ NC='\033[0m' # No Colo
  
  # Default values
  MASTODON_PATH="${MASTODON_PATH:-}"
- ADD_VARIATIONS=""
- SET_DEFAULT=""
+ ADD_VARIATIONS="n"
++SET_DEFAULT="n"
  
  # Get script directory and version
  SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
